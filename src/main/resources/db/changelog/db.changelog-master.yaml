# Liquibase master changelog — includes every file under changes/.
# Keep spring.liquibase.enabled=false while relying on JPA ddl-auto.
databaseChangeLog:
  - includeAll:
      path: db/changelog/changes
      errorIfMissingOrEmpty: false
